---

# 一、你要准备的两套核心输入

## 1. Word 模板

这个负责：

* 报告最终长什么样
* 哪些位置放哪一段文字
* 哪些位置放图表
* 哪些位置放表格
* 标题、页眉页脚、样式

这里面你要先把占位符设计好，比如：

* `{{market_summary}}`
* `{{industry_tracking}}`
* `{{risk_section}}`
* `{{chart_1}}`
* `{{table_1}}`

也就是说：

**Word 模板定义“放哪里”。**

---

## 2. Section 配置 / Prompt 配置文件

这个负责：

* 每个占位符要写什么
* 字数要求
* 禁用词
* 是否允许个股名
* 这一段依赖哪些数据
* 这一段用哪个 retrieval profile
* 这一段的 prompt 是什么
* 这一段怎么校验

也就是说：

**配置文件定义“写什么、怎么写、用什么写”。**

---

# 二、程序实际做的事情

你把这两个东西交给程序之后，程序就按一套流水线跑：

## Step 1：读取 Word 模板

识别里面有哪些占位符。

## Step 2：读取 section 配置文件

知道每个占位符对应哪个 section。

## Step 3：按 section 去取数据

可能来自：

* Excel 数据底稿
* 数据库
* RAG 检索结果
* 事件抽取结果
* 图表/表格配置

这里的 `query` 更准确地说是 **检索 Query**：

* 先用它去数据库、新闻库或 RAG/混合检索系统里找事实材料
* 检索范围可以由报告周期限定，比如最近一周
* 检索结果作为 `evidence_context`
* 后续生成只能基于这些事实材料写作，避免模型自由发挥和幻觉

也就是说，`query` 不等于最终生成 prompt。它负责“找事实”。

## Step 4：组装 prompt

对每个 section：

* 加上 section 规则
* 加上 data slots
* 加上 evidence context
* 形成最终 prompt

## Step 5：逐段生成

生成：

* `market_summary`
* `industry_tracking`
* `risk_section`
  ……

## Step 6：做校验

检查：

* 字数
* 禁用词
* 数字是否正确
* 风格是否客观
* 来源是否可追溯

## Step 7：替换 Word 占位符

把每段文字、图表、表格回填进模板。

## Step 8：导出最终 Word

输出正式报告。

## 当前后端实现口径

当前项目已经把这条链路接到 `report_projects` 工作台：

* `section_config.yaml` 的 `placeholders` 或旧版 `sections` 定义 Word 占位符和 section 规则
* `prompt_templates.md` 的二级标题定义 Prompt 模板，模板内的 `检索 Query` 用来找事实，`写作要求` 用来约束生成
* 工作台调用 `/api/report-projects/{slug}/render-jobs` 提交后台任务，再轮询任务状态；后台执行 `配置解析 -> 数据库 evidence 检索 -> ModelGateway 生成 -> Word 占位符替换 -> runs 日志`
* 同一报告项目只允许一个活动生成任务，重复点击复用已有 job；顶层任务单 worker 执行，本地 embedding/reranker 加载与推理由锁保护
* 任务状态保存在当前 AlphaFoundry 进程中，应用重启后运行中任务不会续跑；已生成 Word/PPT 和 runs 日志仍保留
* 模型调用走 `ModelGatewayImpl.chat`，优先使用 `TASK_REPORTING_*`；未配置 reporting 路由时回落到 `TASK_DEFAULT_*`
* 当前检索先采用轻量关键词检索，覆盖 `ingestion_queue_item.raw_content/title` 和 `canonical_event.summary`，后续可以替换为混合检索、向量召回和 rerank

---

# 三、你理解成“两个文件”也可以，但更准确是“三层”

你刚刚说：

* 一个 Word 模板
* 一个每个章节对应 prompt 的文件

这个理解基本是对的。
但更准确一点，建议拆成 **三层**：

## 1. Word 模板文件

负责版式和占位符

## 2. Section 配置文件

负责 section 的业务规则

## 3. Prompt 模板文件

负责 prompt 的统一格式

这样会比把所有东西都塞进一个 prompt 文件更好维护。

---

# 四、最推荐的文件组织方式

我建议你最后做成这样：

## A. Word 模板

比如：

```text id="3j3pj"
templates/weekly_report_template.docx
```

---

## B. Section 配置文件

比如：

```json id="57f5s"
{
  "report_type": "weekly_report",
  "sections": [
    {
      "section_id": "market_summary",
      "placeholder": "{{market_summary}}",
      "title": "市场综述",
      "generation_mode": "strict_template",
      "max_words": 250,
      "forbidden_terms": ["基金", "ETF"],
      "allow_company_names": false,
      "data_slots": [
        "sh_index_weekly_return",
        "sz_index_weekly_return",
        "turnover_change"
      ],
      "derived_slots": [
        "market_direction_phrase",
        "risk_appetite_phrase"
      ],
      "retrieval_profile": null,
      "prompt_template": "market_summary_v1"
    },
    {
      "section_id": "industry_tracking",
      "placeholder": "{{industry_tracking}}",
      "title": "行业跟踪",
      "generation_mode": "evidence_grounded_generation",
      "max_words": 400,
      "forbidden_terms": ["买入", "强烈推荐"],
      "allow_company_names": false,
      "data_slots": ["industry_index_return"],
      "derived_slots": [],
      "retrieval_profile": "weekly_report_profile",
      "prompt_template": "industry_tracking_v1"
    }
  ]
}
```

---

## C. Prompt 模板文件

比如：

```json id="b9rse"
{
  "market_summary_v1": {
    "system_role": "你是一名基金公司研究员，正在撰写正式周报。",
    "section_purpose": "总结本周市场整体表现与风险偏好变化。",
    "constraints": [
      "不得出现ETF名称",
      "不得出现基金名称",
      "必须使用给定精确数值",
      "语言客观、审慎"
    ],
    "output_format": "只输出一段完整文字，不要解释。"
  },
  "industry_tracking_v1": {
    "system_role": "你是一名基金公司行业研究员，正在撰写行业周报。",
    "section_purpose": "总结本周行业景气变化和核心驱动。",
    "constraints": [
      "不得包含投资建议",
      "只能基于给定证据写作",
      "语言需专业、客观"
    ],
    "output_format": "只输出一段完整文字，不要解释。"
  }
}
```

---

# 五、为什么不要只放“每个章节一个 prompt”

因为如果你只是做成：

* `market_summary_prompt.txt`
* `industry_tracking_prompt.txt`

以后会很快乱掉：

* 哪个 prompt 对应哪个占位符
* 哪个 prompt 对应哪个模板版本
* 哪些段要用数据槽
* 哪些段要走 RAG
* 哪些段不许写个股

这些逻辑会散在各个地方。

所以最好是：

## section config 负责业务规则

## prompt template 负责语言模板

## Word 模板负责最终承载

---

# 六、你的程序最后大概就是这样工作

你可以把程序想成一个 orchestrator：

```python id="3h9x3"
def generate_report(word_template, section_config, prompt_templates, excel_data, retrieval_system):
    for section in section_config["sections"]:
        data_context = load_data_slots(section, excel_data)
        evidence_context = load_evidence(section, retrieval_system)

        prompt = build_prompt(
            section=section,
            prompt_template=prompt_templates[section["prompt_template"]],
            data_context=data_context,
            evidence_context=evidence_context
        )

        text = generate_section_text(prompt)
        validated_text = validate_section_text(section, text, data_context, evidence_context)

        replace_placeholder(word_template, section["placeholder"], validated_text)

    return export_docx(word_template)
```

这就是你整个系统的核心逻辑。

---

# 七、你现在最应该自己先做好的东西

你不需要先把所有 prompt 都写得特别完美，
但你需要先把这些东西定下来：

## 1. Word 模板里的占位符

先确定一份周报都有哪些固定 section。

## 2. 每个 section 的职责

比如：

* 市场综述
* 行业景气
* 重点事件
* 风险提示

## 3. 每个 section 依赖什么

是 Excel 数据、RAG 检索，还是两者都有。

## 4. 每个 section 的生成模式

* strict_template
* semi_template
* evidence_grounded_generation

这四件事比“prompt 写得多漂亮”更重要。

---

# 八、你可以怎么开始最省力

最推荐的是：

## 第一步

先做一版 Word 模板

## 第二步

先做一版 section config

## 第三步

prompt 先不用写很多，只先做 3 个 section：

* 市场综述
* 行业跟踪
* 风险提示

## 第四步

先把流程跑通，再慢慢加 section

不要一开始就想把几百篇历史报告全部抽成 prompt。

---

# 九、最短回答

对，你理解得对。

你最后应该是准备：

### 1. 一个 Word 模板文件

定义占位符和最终排版

### 2. 一个 section 配置文件

定义每个占位符对应什么 section、用什么数据、怎么生成

### 3. 一个 prompt 模板文件

定义每个 section 的 prompt 框架

然后把这些交给程序，程序就会按流程：

**取数据 → 检索证据 → 组装 prompt → 逐段生成 → 替换占位符 → 输出最终 Word**

这就是最适合你现在这个项目的做法。

下一步最适合就是我直接帮你写一份
**“周报 section_config.json 示例 + prompt_templates.json 示例 + 占位符命名规范”**。
