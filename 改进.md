我的建议：**不要把每个数据源写成 Skill，也不要一开始就写成 MCP。核心应该先写成“Python Package 里的 Data Connector 插件模块”，然后在外层暴露 CLI；MCP 只是给 Agent 调用这些能力的一层薄封装。**

也就是三层结构：

```text
Python Package / Core Connector
        ↓
CLI / Scheduler / Pipeline 调度
        ↓
MCP Tool / Agent Skill / 前端按钮
```

## 1. 结论：数据源本体应该是 Python 包，不是 Skill

你的项目是投研系统，数据源很多，比如 AkShare、Yahoo、Wind、巨潮、指数官网、新闻源、公告 PDF、研报 PDF、行业数据、网页爬虫等。每个数据源最重要的是：

```text
能不能稳定获取
能不能断点续跑
能不能限速重试
能不能标准化字段
能不能记录 provenance 来源
能不能入库
能不能被回测/事件提取/前端复用
```

这些都属于 **工程运行时代码** ，应该放在 Python 包里，而不是放在 Agent 的 Skill 里。

Skill 更像是“教 AI 怎么做某类任务的说明书 + 少量脚本”，比如：

```text
如何新增一个数据源
如何检查一个 connector 是否合格
如何把公告 PDF 转 Markdown
如何跑一次新闻抓取并排查失败
```

但 Skill 不应该承载核心业务逻辑。否则后面 CLI、前端、定时任务、回测模块都很难稳定调用。

---

## 2. 我推荐的分层

### A. Core：数据源 Connector，写成 Python Package

每个数据源一个 connector，比如：

```text
alphafoundry/
  connectors/
    base.py
    registry.py
    akshare/
      connector.py
      schema.py
      config.yaml
    yahoo/
      connector.py
      schema.py
      config.yaml
    cninfo/
      connector.py
      schema.py
      config.yaml
    csindex/
      connector.py
      schema.py
      config.yaml
    szse/
      connector.py
      schema.py
      config.yaml
```

每个 connector 都实现统一接口：

```python
class BaseConnector:
    name: str
    source_type: str

    def discover(self, **kwargs):
        """发现可抓取对象，比如指数列表、公告列表、新闻列表。"""

    def fetch(self, item):
        """获取原始数据。"""

    def parse(self, raw):
        """解析为结构化或半结构化数据。"""

    def normalize(self, parsed):
        """转成系统统一 schema。"""

    def validate(self, records):
        """字段校验、空值校验、主键校验。"""

    def save_raw(self, raw):
        """保存原始数据，便于追溯。"""

    def upsert(self, records):
        """写入数据库。"""
```

这样你的系统不会被某个数据源绑定。AkShare 崩了，可以换 Tushare/官网爬虫；Yahoo 被限流，可以换 Stooq/交易所源；公告 PDF 格式变了，也只改对应 connector。

---

### B. CLI：给你自己、服务器、定时任务调用

CLI 是非常应该做的，而且应该尽早做。比如：

```bash
alphafoundry ingest akshare --dataset stock_daily --start 2024-01-01
alphafoundry ingest cninfo --type announcement --date 2026-06-02
alphafoundry ingest csindex --index 000300 --dataset constituents
alphafoundry validate csindex
alphafoundry backfill news --from 2025-01-01 --to 2026-06-02
```

CLI 的作用是把 connector 变成稳定的“可运行任务”。后续你可以：

```text
本地手动跑
服务器 cron 定时跑
Airflow/Prefect 调度
前端点击按钮触发
Agent 调用
CI/CD 测试
```

所以 CLI 是连接工程系统和自动化系统的关键层。

---

### C. MCP：给 Agent 调用，不要把 MCP 当核心代码

MCP 适合暴露高层动作，比如：

```text
run_data_ingestion(source="cninfo", date="today")
check_connector_status(source="akshare")
search_events(stock_code="600519")
trigger_backtest(strategy_id="low_vol")
```

但 MCP 不应该直接写复杂抓取逻辑。正确做法是：

```text
MCP Tool 调用 Python Package / CLI
```

例如：

```python
@mcp.tool()
def ingest_source(source: str, dataset: str, date: str):
    return run_cli_or_python_function(source, dataset, date)
```

这样 MCP 只是 Agent 入口。哪天你不用 Claude/OpenClaw/ChatGPT Agent 了，核心系统仍然能跑。

---

## 3. Skill、MCP、Python 包、CLI 分别适合什么

| 形式           | 适合放什么                                              | 不适合放什么        |
| -------------- | ------------------------------------------------------- | ------------------- |
| Python Package | 核心业务逻辑、connector、parser、schema、入库、校验     | 给 AI 的长说明      |
| CLI            | 可执行任务、批处理、回填、调度、测试                    | 复杂业务逻辑本身    |
| MCP            | 给 Agent 调用的工具接口                                 | 核心爬虫/数据库逻辑 |
| Skill          | 工作流说明、开发规范、排错 SOP、让 Agent 知道怎么用系统 | 生产级数据接入代码  |

你的情况最合理的是：

```text
数据源 = Python connector
运行入口 = CLI
Agent 可调用 = MCP
AI 开发规范 = Skill
```

---

## 4. 每个数据源应该封装到什么粒度？

不要按“网站”粗暴封装，也不要按“字段”太细。建议按 **source + dataset** 封装。

比如中证指数网：

```text
source: csindex
datasets:
  - index_basic
  - index_constituents
  - index_valuation
  - index_weight
  - index_industry_exposure
```

巨潮资讯：

```text
source: cninfo
datasets:
  - announcements
  - annual_reports
  - semi_annual_reports
  - prospectus
```

AkShare：

```text
source: akshare
datasets:
  - stock_daily
  - index_daily
  - board_concept
  - market_breadth
  - limit_up_pool
```

新闻源：

```text
source: news
datasets:
  - realtime_news
  - company_news
  - macro_news
  - sector_news
```

这样每个数据源可以统一管理，但不同数据集可以独立跑、独立失败、独立重试。

---

## 5. 我建议你的标准 Connector 输出格式

你的项目后面要做：

```text
断言/事件提取
影响传导
回测
前端实时展示
RAG
溯源
```

所以每条数据最好都有统一元信息：

```python
{
    "source": "cninfo",
    "dataset": "announcements",
    "entity_type": "stock",
    "entity_id": "000001.SZ",
    "title": "平安银行股份有限公司公告",
    "content": "...",
    "published_at": "2026-06-02T09:30:00+08:00",
    "fetched_at": "2026-06-02T09:35:12+08:00",
    "url": "...",
    "raw_path": "s3://raw/cninfo/...",
    "content_hash": "...",
    "version": "v1",
    "metadata": {
        "file_type": "pdf",
        "exchange": "SZSE",
        "announcement_type": "annual_report"
    }
}
```

这比简单存一个 DataFrame 强很多。因为你后面一定会遇到：

```text
这条事件是从哪里来的？
原始 PDF 是哪个？
什么时候抓的？
有没有重复？
是不是更新过？
字段是哪一版 parser 解析出来的？
为什么前端显示和回测不一致？
```

统一 schema 能解决这些问题。

---

## 6. 项目里可以定义一个 Connector Registry

你可以有一个注册中心：

```python
CONNECTOR_REGISTRY = {
    "akshare": AkshareConnector,
    "yahoo": YahooConnector,
    "cninfo": CninfoConnector,
    "csindex": CsindexConnector,
}
```

CLI 或 MCP 只需要调用：

```python
connector = get_connector("cninfo")
connector.run(dataset="announcements", date="2026-06-02")
```

这样新增数据源时不需要改很多地方。

---

## 7. 推荐目录结构

我建议你后续往这个结构靠：

```text
alphafoundry/
  core/
    config.py
    logging.py
    retry.py
    rate_limit.py
    checksum.py
    time.py

  connectors/
    base.py
    registry.py

    akshare/
      connector.py
      schemas.py
      tests.py

    cninfo/
      connector.py
      parser.py
      schemas.py
      tests.py

    csindex/
      connector.py
      parser.py
      schemas.py
      tests.py

  pipelines/
    ingest.py
    normalize.py
    extract_assertions.py
    extract_events.py
    propagate_impact.py
    backtest.py

  storage/
    raw_store.py
    postgres.py
    duckdb.py
    vector_store.py

  cli/
    main.py

  mcp/
    server.py
    tools.py

  skills/
    add_new_connector.md
    debug_data_ingestion.md
    run_daily_pipeline.md
```

核心逻辑都在 `connectors/`, `pipelines/`, `storage/`。
`cli/` 和 `mcp/` 都只是入口。
`skills/` 是给 Agent 或 Claude Code 看的操作说明。

---

## 8. 什么时候需要 MCP？

你现在不需要一开始把每个数据源都做 MCP。应该等这些能力稳定后再暴露 MCP：

```text
ingest_source
list_available_datasets
check_ingestion_status
rerun_failed_jobs
query_latest_events
trigger_backtest
summarize_connector_health
```

MCP 更适合“动作级别”，不是“数据源内部函数级别”。

不要给 Agent 暴露这种太底层的接口：

```text
fetch_page()
parse_html()
save_raw_file()
```

而应该暴露：

```text
抓取今天所有公告
检查中证指数成分股是否更新
重跑失败的数据源任务
查询某只股票最近事件
```

---

## 9. Skill 应该怎么写？

Skill 可以写，但写的是 **开发规范和工作流** ，比如：

```text
skills/
  data-connector-development.md
  mcp-tool-development.md
  ingestion-debugging.md
  frontend-workflow.md
```

例如 `data-connector-development.md` 里可以规定：

```text
新增数据源必须：
1. 继承 BaseConnector
2. 实现 discover/fetch/parse/normalize/validate/upsert
3. 保存 raw 数据
4. 生成 content_hash
5. 写入 ingestion_runs 表
6. 至少提供 smoke test
7. 添加 CLI 命令
8. 不允许直接在前端调用外部数据源
```

这样 Claude Code / OpenClaw 帮你写代码时，会更稳定，不会乱改架构。

---

## 10. 我给你的最终建议

你的 AlphaFoundry 应该采用：

```text
Python Package 作为核心
CLI 作为统一执行入口
MCP 作为 Agent 工具层
Skill 作为开发/运维规范
```

不要选其中一个，而是分层使用。

最重要的是先定义一个统一的 `BaseConnector` 和 `DataRecord` schema。之后所有数据源都接这个标准。只要这一步做好，你后面的实时显示、事件提取、影响传导、回测、RAG 都会顺很多。

我建议下一步先做三个文件：

```text
docs/architecture/data-connectors.md
alphafoundry/connectors/base.py
alphafoundry/connectors/registry.py
```

然后先把一个最简单的数据源，比如 AkShare 或中证指数网，改造成标准 connector，作为模板。
