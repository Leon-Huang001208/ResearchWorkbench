(function () {
  'use strict'; const app=window.AlphaPrototype;
  function render(state){ const root=document.querySelector('#tweak-panel'); if(!root)return; if(!state.tweaksOpen){root.innerHTML='<button class="tweak-launch" data-tweak="open" aria-label="打开原型设置">⌘</button>';return;} root.innerHTML=`<section class="tweak-card"><div class="card-header"><strong>原型状态</strong><button class="icon-button" data-tweak="close">×</button></div><label class="tweak-row">主题<select data-tweak="theme"><option value="light">浅色</option><option value="dark">深色</option></select></label><label class="tweak-row">密度<select data-tweak="density"><option value="compact">紧凑</option><option value="comfortable">舒适</option></select></label><label class="tweak-row">页面状态<select data-tweak="state">${app.demoStates.map((item)=>`<option value="${item}" ${state.demoState===item?'selected':''}>${item}</option>`).join('')}</select></label><button class="button small" data-tweak="research">切换研究空间</button><div class="divider"></div><span class="module-label">五条可点击旅程</span><div class="chip-row">${['J01','J02','J03','J04','J05'].map(id=>`<button class="button small" data-start-journey="${id}">${id}</button>`).join('')}</div></section>`; root.querySelector('[data-tweak="theme"]').value=state.theme; root.querySelector('[data-tweak="density"]').value=state.density; }
  document.addEventListener('click',(event)=>{ const action=event.target.closest('[data-tweak]')?.dataset.tweak; if(action==='open'||action==='close')app.setState({tweaksOpen:action==='open'}); if(action==='research')app.setState({researchSpaceOpen:!app.state.researchSpaceOpen}); });
  document.addEventListener('change',(event)=>{ const action=event.target.dataset.tweak; if(action==='theme'){document.documentElement.dataset.theme=event.target.value;app.setState({theme:event.target.value});} if(action==='density'){document.documentElement.dataset.density=event.target.value;app.setState({density:event.target.value});} if(action==='state'){app.setState({demoState:event.target.value});app.router.refresh();} });
  app.subscribe(render); window.AlphaTweaks={init(){render(app.state);}};
})();
