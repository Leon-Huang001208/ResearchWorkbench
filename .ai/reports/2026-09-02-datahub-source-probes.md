# DataHub公开来源只读核对

本记录属于任务 `task-75e24928-datahub`，用于验证取数协议与产品验收的独立对照，不是DataHub已经上线的证明。没有读取API密钥或调用付费服务，没有执行远端JavaScript。

## NAV协议与独立对照

现有Python/httpx，`trust_env=False`，固定HTTPS路径 `https://api.fund.eastmoney.com/f10/lsjz`，Referer为对应基金净值页，不重定向，不登录。

- 基金000001、日期2025-01-01..2025-12-31，响应顶层 `TotalCount=243`、`PageSize=20`，Data.LSJZList包含FSRQ/DWJZ/LJJZ/JZZZL/FHSP等。
- 请求pageSize100，实际仍返回PageSize20；依次请求pageIndex1..13得到243行/243个唯一日期，耗时0.82秒（只代表本次网络响应）。不能以返回20<请求100判断结束。
- 首条2025-01-02，单位净值0.8030；末条2025-12-31，单位净值1.0830。
- Decimal独立计算首末观测区间未复权单位净值变化 `0.348692403486924034869240349`；按逐点历史峰值计算该观测窗口未复权净值最大回撤 `-0.1247803163444639718804920914`。这是验收计算口径，不是投资建议，也不是完整日历年度总回报。
- 2025-09-22原始行包含 `FHSP=每10份派现金0.1000元`、`FHFCZ=0.01`，进一步说明不能把单位净值变化直接作为含分红总回报。
- 另查2023-01-01..2025-12-31，总量729；只读前11页得到220条，用于验证分页而非完整三年验收。不得将220条标记为完整。

## 补充资料

- `https://fundf10.eastmoney.com/jbgk_000001.html`：HTTP200，约50KB，table.info为字段/原文值。包括份额代码、类型、成立日期、规模、费用和基准说明。基准文字不是基准净值序列；当前页面的规模截止2026-06-30，不能作为2025年末时点事实。
- `https://fundf10.eastmoney.com/fhsp_000001.html`：HTTP200，约47KB，25条分红；表头权益登记日/除息日/每10份分红/发放日。另表明确暂无拆分信息，空表不是网络失败。
- `https://fundf10.eastmoney.com/FundArchivesDatas.aspx`：type=jjcc、code=000001、topline=100、year=2025、month空，HTTP200，约97KB。返回apidata对象中的HTML字符串，未执行脚本。h4给报告季度/截止日，表给股票代码/名/占净值比例/持股数万股/市值万元。只代表来源披露范围，不保证基金全部头寸。

## 实际命令方式

使用现有 `/Users/leon/Desktop/Projects/AlphaFoundry-runtime-agnostic-core/.venv/bin/python -c ...`，调用httpx与BeautifulSoup，解析内容仅打印协议、字段和上述汇总。完整执行代码及stdout保存在本任务工具记录。

基线回归：Python76 passed in12.88s；JS32 passed、0skip。原始输出在隔离工作树 `logs/datahub-baseline-pytest.log` 与 `logs/datahub-baseline-js.log`。这些是修改前基线，不代替最终回归。

## 授权与边界

用户明确允许主代理代为确认本轮基金000001历史净值、基本资料、分红、已披露持仓四类公开只读查询的DSH审批。其他来源、费用、账号、附件/聊天外发未获新增授权。
真实产品查询、共享资料、子Agent与报告文件后续已执行，证据独立记录在[本批验收](2026-09-02-datahub-acceptance.md)；本文件仍仅代表协议与解析探针。

## 新Provider实现的真实解析探针

2026-09-02调用本工作树 `providers.fetch(Query(...))`，未经过Web/原生审批桥接，因此仍不是完整产品验收：

- 2025 NAV：complete，243行、13页、provider_total243；首末日期/净值与独立对照一致。
- 基本资料：snapshot，15字段；未识别资产规模字段，已反馈实现代理核对原文标题。
- 分红：snapshot，25条；最新2025-09-22每10份0.1000元。
- 2025持仓：failed，holdings_table_missing。进一步固定公开请求确认表存在，真实标题是 `占净值<br/>比例`，文本提取产生空格，严格标题匹配漏掉。已交实现代理先补真实结构回归再修复，不能把此次失败说成数据不存在。
- 基本资料另核对：规模标签实际为“净资产规模”；源HTML存在缺闭合td，直接get_text会把基金类型混入基金代码、把份额规模混入净资产规模。销售服务费率为`---（每年）`，应保留原文但数值视为缺失，不是零。已提供真实最小HTML片段给实现代理补回归。

修复后再次调用真实Provider：基本资料snapshot16字段，代码/规模不再串位，销售服务费率null且原文保留；2025持仓snapshot220行，四个季度截止日均存在。最新季度首行300308、占净值4.57%、22.00万股、13,420.00万元，与源表一致。仍不能视为完整头寸或披露时间已取得。上述解析问题已通过真实重测，不代表Web端到端已完成。
