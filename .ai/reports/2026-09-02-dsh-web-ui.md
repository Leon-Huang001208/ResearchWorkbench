# 2026-09-02 Research Web UI

## 任务与隔离

- 工作树：`/Users/leon/Desktop/Projects/ResearchWorkbench/.worktrees/dsh-web-ui`
- 分支：`codex/dsh-web-ui`
- 所有权：`app/research_web/ui/**`、`tests/javascript/research_web_ui.test.mjs`、本文与 `docs/research-web-ui.md`。
- 未修改后端、旧 Web、市场、日程、桌面与其他代理负责的文件。未新增依赖。

## 已实现

原型视觉语言迁入独立原生 Research Web；FinGPT / Claw / 历史 / Skills / 设置均对接真实 `/api/research` 契约。包括新建与多轮消息、模型配置、历史/重命名/取消、附件上传、实际产出文件、安全 Markdown、Agent 活动、审批和提问、升级草稿与响应式面板。

没有演示数据、虚构进度、模拟降级、Claim/Evidence/Quality Gate 或旧研究管线。Key 不进入浏览器持久存储/日志。上传不会被描述为已经完成模型读取。

## 测试证据

遵循 `test-driven-development`：首次 11 个目标测试在实现缺失时全部失败；实现后全部通过。第二轮增加旧快照竞态、创建导航竞态及升级草稿测试，先观察到 3 个失败，修复后 16 个测试通过。

- `node --test tests/javascript/research_web_ui.test.mjs`：16 / 16 通过。
- `node --test tests/javascript/*.test.mjs`：29 / 29 通过（含已有蓝图/原型测试）。
- `node --check` 检查 `app.mjs`、`core.mjs`、`views.mjs`、`markdown.mjs`：全部通过。
- `git diff --check`：通过。

测试使用真实生产纯函数/控制器；网络/SSE 边界使用测试响应，不作为生产演示数据。覆盖 Markdown 与链接安全、同源文件 sandbox、错误保留输入、消息幂等、multipart 文件上传、秘密不记录、旧会话响应隔离、重连不重发、升级不自动发送。

## 未验证与交接

- 未运行浏览器视觉/键盘/移动端与真实 DSH 端到端验证：父任务集成后执行。
- 未验证真实 provider 凭据、工具沙箱、模型附件读取、子 Agent 执行：取决于后端能力，UI 仅呈现返回状态。
- 未运行 Python 检查：无 Python 源码变更。
- 未验证 Windows 桌面：此次仅独立 Web UI，不涉及桌面交付。
- 父任务负责主项目 Harness 最终硬门、全局模块/架构索引更新与集成审查。
