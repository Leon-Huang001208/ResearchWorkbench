是的， **要统一接口，但不要强行统一成同一种数据模型** 。

更准确地说：

```text
文档数据和市场数据应该统一“接入接口 / 元信息 / 任务调度 / 溯源 / 状态管理”
但不要统一“业务数据 schema”
```

也就是：**外层统一，内层分型。**

---

## 1. 为什么要统一接口？

因为不管是公告 PDF、研报、新闻，还是行情、估值、成分股，本质上都需要经过类似流程：

```text
发现数据 → 获取原始数据 → 解析 → 标准化 → 校验 → 存储 → 下游处理
```

所以它们应该共享一套基础接口：

```python
class BaseConnector:
    def discover(self, request): ...
    def fetch(self, item): ...
    def parse(self, raw): ...
    def normalize(self, parsed): ...
    def validate(self, records): ...
    def persist(self, records): ...
```

这样 CLI、MCP、调度系统、监控系统都可以统一调用：

```bash
alphafoundry ingest cninfo announcements --date 2026-06-02
alphafoundry ingest akshare stock_daily --date 2026-06-02
alphafoundry ingest csindex constituents --date 2026-06-02
```

从调度视角看，它们都是 `ingest job`。
从业务视角看，它们才分成文档、行情、指数、新闻、事件等类型。

---

## 2. 但不能把文档数据和市场数据强行做成同一个表

文档数据是非结构化/半结构化：

```text
PDF、HTML、Word、Markdown、公告、研报、新闻、纪要
```

它的核心字段是：

```text
标题、正文、段落、页码、来源、发布时间、关联公司、embedding、chunk、摘要、断言、事件
```

市场数据是结构化时间序列：

```text
OHLCV、成交额、换手率、PE、PB、股本、成分股、权重、行业分类
```

它的核心字段是：

```text
交易日、证券代码、指标名、数值、频率、复权方式、币种、数据版本
```

如果硬统一成一个 `DataRecord` 大表，会变成这种怪物：

```text
title 有时候为空
content 有时候为空
open/high/low/close 有时候为空
page_no 有时候为空
metric_value 有时候为空
chunk_id 有时候为空
```

最后会很难维护。

---

## 3. 推荐设计：统一 Envelope，分开 Payload

我建议你的系统采用这个设计：

```python
class IngestionRecord:
    source: str
    dataset: str
    asset_type: str       # document / market / fundamental / index / news
    entity_type: str      # stock / index / fund / sector / macro / unknown
    entity_id: str | None
    published_at: datetime | None
    fetched_at: datetime
    raw_uri: str
    content_hash: str
    schema_version: str
    payload: dict
```

这里的 `IngestionRecord` 是统一外壳。
真正的数据放在 `payload` 里。

例如文档数据：

```json
{
  "source": "cninfo",
  "dataset": "announcement",
  "asset_type": "document",
  "entity_type": "stock",
  "entity_id": "000001.SZ",
  "published_at": "2026-06-02T08:30:00+08:00",
  "raw_uri": "s3://raw/cninfo/2026/06/02/xxx.pdf",
  "content_hash": "abc123",
  "payload": {
    "title": "平安银行股份有限公司年度报告",
    "doc_type": "annual_report",
    "file_type": "pdf",
    "text": "...",
    "pages": 188,
    "chunks": [...]
  }
}
```

市场数据：

```json
{
  "source": "akshare",
  "dataset": "stock_daily",
  "asset_type": "market",
  "entity_type": "stock",
  "entity_id": "000001.SZ",
  "published_at": "2026-06-02T15:00:00+08:00",
  "raw_uri": "s3://raw/akshare/stock_daily/2026-06-02.parquet",
  "content_hash": "def456",
  "payload": {
    "trade_date": "2026-06-02",
    "open": 10.12,
    "high": 10.35,
    "low": 10.01,
    "close": 10.28,
    "volume": 123456789,
    "amount": 987654321.0,
    "adjustment": "none"
  }
}
```

这个模式的好处是：**调度、日志、溯源统一；业务数据各自专业化。**

---

## 4. 更工程化的做法：BaseConnector + Typed Connector

可以这样分：

```python
class BaseConnector:
    source: str

    def discover(self, request): ...
    def fetch(self, item): ...
    def save_raw(self, raw): ...
    def get_metadata(self, raw): ...


class DocumentConnector(BaseConnector):
    def parse_document(self, raw): ...
    def chunk(self, document): ...
    def extract_entities(self, document): ...
    def embed(self, chunks): ...


class MarketDataConnector(BaseConnector):
    def parse_table(self, raw): ...
    def normalize_bars(self, table): ...
    def adjust_prices(self, table): ...
    def validate_time_series(self, table): ...
```

也就是：

```text
BaseConnector 统一生命周期
DocumentConnector 处理非结构化
MarketDataConnector 处理结构化时间序列
```

你的具体数据源再继承它们：

```python
class CninfoAnnouncementConnector(DocumentConnector):
    ...


class ResearchReportConnector(DocumentConnector):
    ...


class AkshareDailyBarConnector(MarketDataConnector):
    ...


class CsindexConstituentConnector(MarketDataConnector):
    ...
```

---

## 5. 数据库存储也应该分开

我建议不要所有数据都塞进 PostgreSQL 一张表。更合理是：

```text
Raw Layer:
  原始 PDF / HTML / JSON / CSV / Parquet
  存 MinIO / S3 / 本地对象存储

Metadata Layer:
  ingestion_runs
  data_sources
  raw_objects
  document_metadata
  market_dataset_metadata
  存 PostgreSQL

Structured Layer:
  行情、估值、成分股、财务指标
  存 PostgreSQL / TimescaleDB / ClickHouse / DuckDB / Parquet

Document Layer:
  文档正文、chunk、embedding、实体、摘要
  存 PostgreSQL + pgvector / Milvus / Qdrant

Event Layer:
  断言、事件、影响链、因果关系
  存 PostgreSQL / Graph DB
```

可以理解为：

```text
文档数据最终服务于：RAG、事件提取、断言、摘要、问答
市场数据最终服务于：行情展示、回测、指标计算、因子分析
```

两者都进入同一个“投研知识系统”，但底层存储形态不一样。

---

## 6. 对你项目最关键的是统一“实体 ID”

文档和市场数据真正需要打通的，不是表结构，而是实体体系。

比如：

```text
000300.SH  沪深300指数
000001.SZ  平安银行
600519.SH  贵州茅台
食品饮料    行业
红利低波    指数主题
```

文档数据里提到“贵州茅台”，市场数据里是 `600519.SH`，指数成分股里也是 `600519.SH`，事件里也应该落到同一个 `entity_id`。

所以你应该有统一的实体主表：

```text
dim_entity
  entity_id
  entity_type
  name_cn
  name_en
  ticker
  exchange
  wind_code
  akshare_code
  yahoo_symbol
  cninfo_code
  aliases
```

这个比统一文档和行情表更重要。

---

## 7. 我的建议架构

你可以这样设计：

```text
connectors/
  base.py

  document/
    base.py
    cninfo.py
    research_report.py
    news.py
    pdf_folder.py

  market/
    base.py
    akshare.py
    yahoo.py
    wind.py
    csindex.py

  registry.py
```

接口统一：

```python
connector.run(request)
```

输出统一：

```python
IngestionResult(
    source="cninfo",
    dataset="announcements",
    asset_type="document",
    raw_objects=[...],
    records=[...],
    stats={...}
)
```

但 records 是分型的：

```python
DocumentRecord
MarketBarRecord
IndexConstituentRecord
ValuationRecord
NewsRecord
EventRecord
```

---

## 8. 最终判断

你的直觉是对的：**文档数据和市场数据需要统一接入接口。**

但要注意这句话：

```text
统一的是 ingestion 生命周期，不是统一成同一个业务 schema。
```

我建议你采用：

```text
BaseConnector
  ├── DocumentConnector
  ├── MarketDataConnector
  ├── FundamentalConnector
  ├── IndexConnector
  └── NewsConnector
```

然后统一输出：

```text
RawObject + Metadata + TypedRecord
```

这样你的系统既有统一工程治理，又不会牺牲不同数据类型的专业性。
