# 视觉证据边界

- 可读图片附件可以作原图证据，但需标记来源、页码或段落、标题、期间、单位和可见的脚注。
- v1 不通过能力脚本裁剪 PDF 原页，不把截图不可用误写成原文没有证据。
- SVG 重绘只表达已定位的节点与关系；标题写“据研报重绘”，图下列入所有来源定位。数值不得因排版被重算或补齐。
- 每条边都需要至少一个非空 `locator`。没有可靠边、关系仅来自猜测，或来源不能定位时，停止生成图，并明确写“视觉证据受限”。
- 不为量化图表合成视觉数据，不用装饰图替代证据。
